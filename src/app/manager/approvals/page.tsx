'use client';
import AppShell from '@/components/AppShell';
import { useApi } from '@/components/ClientTools';
export default function Approvals(){ const {data}=useApi<any>('/api/manager/activation-requests'); async function act(id:string,ok:boolean){ await fetch(ok?'/api/manager/approve-activation':'/api/manager/reject-activation',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({requestId:id,reason:'ตรวจสอบแล้ว'})}); location.reload(); } return <AppShell area="manager"><h1>อนุมัติเปิดใช้งาน</h1><div className="table-wrap"><table><thead><tr><th>รหัส</th><th>ชื่อ</th><th>HUB</th><th>วันเริ่มงาน</th><th>อุปกรณ์</th><th>ดำเนินการ</th></tr></thead><tbody>{(data?.requests||[]).map((r:any)=><tr key={r.id}><td>{r.employee_code}</td><td>{r.employee_name}</td><td>{r.hub_name}</td><td>{r.start_date_input}</td><td>{r.device_info}</td><td className="actions"><button onClick={()=>act(r.id,true)}>อนุมัติ</button><button className="btn-danger" onClick={()=>act(r.id,false)}>ปฏิเสธ</button></td></tr>)}</tbody></table></div></AppShell>}
