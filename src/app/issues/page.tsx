'use client';
import AppShell from '@/components/AppShell';
import { useState } from 'react';
import { Message, useApi } from '@/components/ClientTools';
export default function Issues(){ const [topic,setTopic]=useState('ยอดเงินไม่ถูกต้อง'); const [detail,setDetail]=useState(''); const [msg,setMsg]=useState(''); const {data}=useApi<any>('/api/issues/my'); async function submit(e:any){e.preventDefault(); const j=await (await fetch('/api/issues/create',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({topic,detail})})).json(); setMsg(j.message); if(j.ok) setDetail('')}
 return <AppShell><h1>แจ้งปัญหาข้อมูล</h1><Message text={msg} type={msg.includes('เรียบร้อย')?'ok':'notice'}/><div className="split"><form className="card form" onSubmit={submit}><label>หัวข้อ<select value={topic} onChange={e=>setTopic(e.target.value)}><option>ยอดเงินไม่ถูกต้อง</option><option>จำนวนวันเข้างานไม่ตรง</option><option>มีค่าปรับที่ไม่เข้าใจ</option><option>ข้อมูลส่วนตัวไม่ถูกต้อง</option><option>ยังไม่พบข้อมูลของฉัน</option><option>อื่น ๆ</option></select></label><label>รายละเอียด<textarea rows={5} value={detail} onChange={e=>setDetail(e.target.value)}/></label><button>ส่งคำร้อง</button></form><div className="card"><h2>ประวัติคำร้อง</h2>{(data?.issues||[]).map((it:any)=><p key={it.id}><b>{it.topic}</b><br/><span className="pill">{it.status}</span><br/><span className="muted small">{it.created_at}</span></p>)}</div></div></AppShell>}
