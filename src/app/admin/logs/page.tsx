'use client';
import AppShell from '@/components/AppShell';
import { useApi } from '@/components/ClientTools';
function shortDetail(value:any) { try { const text = typeof value === 'string' ? value : JSON.stringify(value); if (!text) return '-'; return text.length > 220 ? `${text.slice(0, 220)}...` : text; } catch { return '-'; } }
export default function Logs() { const { data, loading, error } = useApi<any>('/api/admin/audit-logs'); const logs = data?.logs || []; return <AppShell area="admin"><div className="page-head"><div><p className="eyebrow">Audit Trail</p><h1>Audit Log</h1><p className="muted">ตรวจสอบประวัติการใช้งานและการกระทำสำคัญในระบบ</p></div></div>{loading && <div className="notice">กำลังโหลด Audit Log...</div>}{error && <div className="notice danger">{error}</div>}<div className="data-card-list">{logs.map((l:any) => <div className="card data-card audit-card" key={l.id}><div className="data-card-top"><div><span className="data-kicker">Action</span><h3>{l.action || '-'}</h3></div><span className="pill">{l.actor_code || '-'}</span></div><div className="data-grid"><div><span>เวลา</span><strong>{l.created_at || '-'}</strong></div><div><span>Target</span><strong>{l.target_type || '-'}{l.target_id ? `: ${l.target_id}` : ''}</strong></div></div><div className="audit-detail"><span>รายละเอียด</span><code>{shortDetail(l.detail_json)}</code></div></div>)}</div></AppShell>; }
